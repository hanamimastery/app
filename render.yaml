# databases:
#   - name: mysite
#     databaseName: mysite
#     user: mysite

services:
  - type: web
    name: Hanamimastery
    env: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec hanami server"
    envVars:
      - key: HANAMI_ENV

      # - key: DATABASE_URL
      #   fromDatabase:
      #     name: mysite
      #     property: connectionString
      # - key: RAILS_MASTER_KEY
      #   sync: false
